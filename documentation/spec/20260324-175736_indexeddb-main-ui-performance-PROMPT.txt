Yeah, give me a little more complete list of proposed engineering steps you want to do. Build it up as a standard documentation spec setup with my prompt captured, as well as the recommendations is what you want to do, your to-do list on that. So follow that full standard, create that in there, and I'll read what you want to do to try and solve both of these problems. If the actual amount of data being held by the app is not actually really much of a problem, it's just that it's doing a whole lot of data reading and processing that it never actually uses for the sake of the UI, that's even easier. I think the biggest data volume-wise, we're already purging, which is the audio files themselves. The other thing to consider is if we should be purging more stuff when we purge the audio files. Is there any data that we have right now that is just dead weight, especially once we've deleted the audio? We don't need it for UI. We don't need it for anything else. Any recommendations there would be great too.
