Okie dokie. I would like you to examine this app. I'm going to describe it basically in generic terms to see if overall the values I'm trying to communicate are things that... you can apply to any application. So I'm literally thinking of two different applications in mind here. And you don't know which one it is. So theoretically, this is all stuff that applies to this application. In general, The natural thing that happens when you talk to an AI agent or even just an actual web developer and tell them what things you want to make. We'll start with the simplest framework as possible. In this case, revealing that this is a progressive web app that is loaded on mobile phones. Generally, iOS is the common thing, but progressive web app loads on phones. and this hypothetical app, um, is working. It's doing the things that it's supposed to do. There are things we want to keep developing on it and changing on it. But in general, the agent in the past, or agents in the past that have developed on this, haven't radically changed or improved the structure at all. um in some cases there's a more of a back-end set of modules that uh i still encourage the idea of having a set of things to keep track of um you know important objects uh things that can be abstracted away from the user interface is what I call back-end stuff. And I prefer to have those as modules defined and broken out into separate groups. This app may or may not have that broken out, but I want you to consider that part of it. The idea is if they are... Things being handled, data being processed, there almost always is some... There's state itself, there's logging. These are generic parts of any application. And so essentially you need to have a discipline. on what things you're breaking out on the back end. I personally think that when you come up with one or more of these, there's generally two types. environmental harnesses you know one module it's a back-end thing but lots of things use it and there might be two of these ones like you know one for database stuff one for logging or something But there are things that might get tapped by lots of other things. But then there's modules that just own a specific thing in a microservice world on a cloud. These would be separate microservices. And each one of these modules has its own interface. And nothing should violate these contracts as the interfaces to these modules. So the first is to figure out the overall task we're trying to do here. I think it's probably good to have a lot of the stuff here represent based off of design pattern, based off of the... primary user critical path the user experience on what we expect people to do most of the time so when a user comes to the application based off of the stated goals of the application what is the most common thing if they do this what is the most complete path that touches as deep into the application as is possible for the sake of making a single critical path, that if that one path works, this app essentially is justified in whatever its purpose is. That should be really well defined. What the experience looks like, how it should behave, the values of why it should behave the way it does. Ultimately there should be a some master integration test or something that is trying to follow that to make sure that that flow works. No matter what we do, that flow works. Now this critical path has front end user interface when you're running it, as well as the pieces you're interacting with are back end pieces. So the back end pieces and the front end pieces. Theoretically, by looking at the critical path, you should be able to essentially talk about the different parts of the system. For back-end pieces, it's a two-phase thing where, you know, I can say on step two of the critical path, you might be first phase as you're loading this user interface. That's going to refer to a front end. In this case, both of these progressive web apps are react based frontends in the progressive web app so the components are ultimately the frontend user interface pieces here so the first phase is what page you know or what component is being viewed potentially what sub components are in there as well involved in this, you know, step or phase, what's on the page, what the user interacts with. Only talking about major components here, you know, I don't want to go down to the fine grain of like buttons and forms and inputs and stuff like that, but just, you know, what are the master components that are part of this critical path? And then the backend phase is that those components probably have a one or more of these backend modules that they're going to connect to. And that phase of the front end connecting to the backend should always be using this API contract that the backend module has. So, the front end, each one of the steps in the critical path will probably have a front end aspect. Sometimes there'll be no back end involved other than, you know, the base things of logging and those ones that are... environmental things like database access, persistence of session or data storage, or just general logging, debugging tools. They probably shouldn't be discussed as part of the critical flow. They're just kind of the plumbing that exists on the back end. But all... other modules, that should be the critical path. And I literally want the critical path to be walked through like a story. You know, something about... you know, the homepage, it loads this experience for the user. You know, it's a landing page that might list something or something. And then there's a critical path might be to create a new something and that would be the front end flow and page for creating something and you could say it would you know need that component to load so that it can show the input for the input of whatever the thing is or whatever experience it is to create one of these things the app is a critical object in the front end and then just to say when it's done it will store the object with backhand piece. And this is literally like a story you could walk somebody through in... I'm thinking in the critical path, simplified view should just be a bulleted list. I'm thinking like a bulleted list, rich bulleted list item is a, you know, paragraph, paragraph, you know, 20 to 50 words, but they should start with a bolded section, which is the essence of what that step is, you know. Step one is something like interact to view the main loading page. Second one is to create or whatever the thing is. So there's a little bolded section at the beginning of each one of these things here. But that series of just should be a numbered list that can walk through the whole critical path. And reading that critical path should be a real quick, simple read. It should be talking about components. front-end components. It should be talking about back-end things they interact with other than the environmental ones. And it should walk through all the different pieces there. That's the first thing. Be able to create a comprehensive core critical path statement. all the way through. We should be able to basically name almost all the main objects that are involved in a critical path. And most of the backend objects are... They are probably going to be mentioned during a critical path. It's possible some of them only come into secondary and tertiary paths. So I think critical path and then at least one other path. There's lots of different things that are usable, totally valid cases. Maybe you have an app that isn't really super simple. There's lots of... different types of things you can be doing, either different phases of user usage. Maybe over the user's life cycle of it, there's an initial critical path to get them started. And then there's later on, maintenance or updates or follow-ups or reviews or something. And those would all be secondary, tertiary, quaternary. critical paths. So first, create all those critical paths and basically describing them all. This is part of the domain-driven design aspect of creating the ubiquitous language. By going through and defining this critical path, using these simple names here, we should have ubiquitous language for front-end and back-end components. So that's the first initial part I want you to go through and create on both of these applications is what is this? For right now, read through might be on there, but it may not be done with this discipline in place. See if you can follow this idea and go in and create this critical path, secondary path, tertiary, quaternary, rarely. But at least a secondary, you know. And the whole theme of these paths are, this is the one thing. If this thing works here, that adds most of the value that we want to give to the user. Secondary paths, like, yes, by adding the secondary path, we're still adding a great amount of value to do this second path. But these are full experiences that a user can experience. See, go through all the way from start to whatever the end page or view or interface is. Okay, that's phase one. Let's get all the critical paths defined, captured, the components. So critical path plus the definitions of what the different components are. So critical, secondary, tertiary. That probably should be in one standard document, the main paths. Then I think... in that same document, the second half after you go through those walkthroughs, should be some brief... walkthrough of what those other components are. And, you know, so if there's a back end, there's three different back end pieces. You should have three different things at the bottom to talk about these are the back end things. Once again, back end things are things that can kind of exist in an abstract sense. outside of the user interface. You know, if this is a database object or probably more collection of them, I don't like a one-to-one having a backend module that is one-to-one with like a table. Usually it's a critical business need or category of things that you can have a handful of and or API contract methods that ultimately help define what that backend is. thing is, is that they're kind of like subdomains, but they're back-end abstract things that are, you know, if you take this outside of the context of it being a progressive web app, or having user interface screens, or whatever, what are those back-end things? So, So define all those. All that stuff should be top half of it, critical path, secondary, tertiary paths if necessary. And then the bottom half of that document is a brief description of each one of them. If it is a back-end thing, maybe even if it's for a front-end thing, if there's main options, just a real kickstarter as to what the main API contract things are, but not exhaustive. What are the main aspects of that so someone can understand at a glance? What are the different, you know, if it's sub-UI components, I could list those. If there's model objects that are in there, I should list those. but the actual different AI methods for the main contract of those modules should be called out there. All right, so that's the general idea behind all of those. That gives the general thing for any application, whether it's built with AI or not. That's great. Second half. This is specifically a guideline I want to follow these enforcements, follow these rules, really good separation on any application that gets built. Second half, there should be a separate markdown file document. And this should be about AI to human beings. Visibility layer or something like that. Essentially, when a human asks someone to build an application, They nearly always just talk about the end result, the screens they want to have, and they don't know enough or usually aren't focused on what is essentially the debugging harnesses. This includes logging of different things, but overall is more of an abstract topic. This involves what I'm terming a Tor. Say that we envision all the different flows in here. There's back-end API methods. There's essentially things that happen through the course of the application. It could be a front-end thing, like a component on the front-end visual thing that it queries some backend thing. And they have the process of asking the backend to get this thing. It might not be a simple query lookup here. Or maybe it is just a query lookup and then it lists this. And common use case at the beginning is it has just a few rows in this visual table, but with extended usage over time, You could have hundreds or thousands of things in here and performance might degrade. There's any number of different problems that may happen eventually. And the answer is always going to be, can I get visibility? for the human to come in and see this part of the system. So if you can imagine any of these applications, we've defined the main pieces in that first document, we've defined them. flows of how things work now imagine that this was all made inside of a big factory our anatomy of the factory here is if it got created like, you know, a compact car, and you want to say, show me this component inside here, it's like, You know, you've got to pull out five different things just to go in and actually see where this pipe is going and how this thing connects to here. But this is a whole lot more than just like a compact car metaphor. This is almost like more like a set of conveyor belts inside of a factory. I don't want to just get into that compact car to see where the hose is going. I want to see the actual water flowing through it. I want to see the thing that I care about, which is, you know, users might be creating objects, or they might be giving input, or they might be storing settings. The thing that users are caring about is something that flows through the pipes, quote-unquote, pipes inside of the application. So the pipes of the application itself have objects in it. If it is a front-end user interface component that is listing all the widgets in a big list here, the objects are the actual objects. widgets that get read from whatever backend state full storage and then get put into memory and then get shown on the front end. If I'm having performance issues on speed because there's hundreds or thousands of entries in this thing now and it's downloading all the backend thing. By me being able to go and peek into that thing to see that pipe is coming through, I see, okay, I now have the payload that just came in here. Wow, that has 20,000 rows in it. And each one of those ones doesn't just have the data I need to visually show this. it's got the entire full depth of it because later on down the line, I see that there's a whole lot of, you know, things that were just pre caching some stuff in here, but I, I can't see what that problem. So this AI to human visibility layer, is something I want essentially to be, if this is a factory, I want there to be nice aisles in the factory that... The AI can easily give the human a tour to say, yeah, here's this thing that goes on here. You know about the front end component. Those are domain driven design ubiquitous language. You know what that is. The whole back end of this is working may not be obvious just from the description of the component on the front end. It could be like that I'm, you know, connecting through this thing and I have this connector to do an asynchronous call to this, which might persist somewhere else or something. That's a piping that needs to be explained. So on the AI tour of that one, it would be literally in this section of the thing. If this is getting a list of objects and loading it into memory and then putting it in here, it needs to actually be able to show, you know, when the robot on a factory floor idea, it needs to be able to say, hey, come over this corner. you can see the objects flying over from here and it has an asynchronous cue thing here to let the interface show a loading spinner until they're finished and this thing has a little indicator when it turns it on. And I'm not thinking that this is a, you know, a big visual factory that you're making inside of the thing. The next thing I want to try and describe is how this can actually be done. It's a set of strategies to try to create debuggable, um, attributes inside of the thing for visibility of an otherwise back-end working system. Firstly, we need to have a specific aspect of every individual part of the components. For every front-end UI component, for every back-end component, maybe in the environmental ones, maybe not, we need to have a... AID aspect that we can add to each one of those. And it's basically a framework that can be turned on and off. The main object of turning it on and off is that by doing observability, it can degrade performance. I don't want to have any degraded performance. If we can. So each one of these needs to have a flag where we turn on or off visibility things. But if I turn it on for a given backend thing, it needs to be able to start gathering metrics on it. So if we're debugging something that involves a front-end UI component on step two of a critical path, you can tell right from the critical path, these are the three things that need to be... examined. So each one of these different front-end components and back-end components needs to have a flag of is instrumentation, AI to human visibility mode turned on. As soon as that is turned on, it needs to then start logging information. So this needs to be part of, I think, any of these applications that are built with AI is to have an adequate logging mechanism that can be recording point-in-time things that happen during the flow of a process. So each one of these ones, if I want to debug a certain problem, it should be which of these levers do I want to throw to turn on which instrumentation things. I think it's okay for the general logging system to maybe be doing some nice low-level, not risking performance logging. And that's fine. But if I want to be able to turn on individual logging things, and I think this might eventually get to a point where... I believe one of the hallmarks of all these AI to human built things is going to be that you have a settings app somewhere in the application where you have a developer mode you can turn on. That developer mode you can turn on, what enable buttons and critical places did to let you debug and look into something. But even those visual differences should probably be gated behind these component things. So I think that turning on developer mode should turn that on, and then it should give you flags to turn on which different aspects of the system. So we're literally naming each one of those components, each one of those backend modules, and by turning them on there, that is the thing that flips the switch. So by flipping that switch, you can turn on the front end. The AI itself can turn those on if it knows we're debugging stuff and it can go enable that. That's the other aspect of AI and human programming is that there's a chance that we're going to have AI be able to run the thing, have this enabled, and have a way for it to get the logs out. That can generally take one of two forms. One, you actually make it so that the human can turn it on, and then they need to have some copy thing, which copies all the logs in a compact enough format. If you're, like, dumping giant objects, it can't have that full object in there. It's got to have some reduced amount, so I can copy and paste it without being a billion tokens. and the human can copy and paste it right into your AI conversation. The other side, though, we need to figure out security in between route which has not been figured out yet should be an ai letting it actually export its own stuff and be able to go just fetch that so theoretically a human can say uh yeah i can say go run the thing and then human can say all right i tried that the ai can then go check to see the session of whatever logging stuff it needs to to check and it itself can pull in the law raw logs there that eventually is going to be something we want to get set up and make it smooth because that allows Retrieval Augmented Generation. Where it can have all the more detailed logs. We don't have to worry about slimming it down as much. We can let the AI browse through the logs that are available. within that. So some automated log sharing ability because users will be using this application on who knows what client and having some integration where we can say, enable this integration, maybe it's something on the app, you actually enable something and give it a key and your app can share the key and it can go read it. But some kind of a log sharing telemetry communication system. But that's the secondary one. The primary that should always exist is the copy. You'll be able to copy detailed logs out to be able to paste it into your AI context. So, each module, front-end or back-end, should have enable AI to human visibility mode. That might degrade the front-end processes. I don't think I will advocate for these kind of debugging things actually showing or changing the visible behavior of the application. For example, so I've done debugging AI in the past, and as soon as I turned on developer mode, it started putting like... you know, status loading one, two, three, four, you know, as a visual div that it added on the page for the user to see. I don't advocate for that. I think all I want is that it's grabbing the data, it's logging the stuff. It's possible. That could be a case if the user specifically wants to see the detailed things here. Another thing that the debugging mode is often commonly to... The main objects that get handled by the system have some kind of a visibility thing. So if it's an object that's in the system and it's in the back end, this is probably a thing for all the back end modules. If they have persisted models. There needs to be a discipline of when the human wants to. So this is probably a whole aspect of the AI to human visibility thing is. Any important objects of the system that are defined in the domain-driven design need some way to browse those raw objects in the back end. And I think we could work out how to make it effective. The natural output is just to dump the JSON right to the screen. this takes a little bit of effort of how to show it effectively on the screen. Because without trying to create a giant list, and sometimes that's happening on an iPhone, and you just can't see almost anything. So it does need to eventually be able to give the full raw field nothing hidden. But maybe those are done with expandable things. Maybe it's view each entry with some kind of a visual image. So it does need to eventually be able to give the full raw field nothing hidden, but maybe those are done with expandable things. Maybe it's view each entry with some kind of a visual layout, and then you can click to view the raw JSON for each one of those ones. But the back-end individual pieces, I think should have a debugging mode. I think when you turn on debugging mode, there should be at least on the home index screen of the application, some kind of a bug icon, emoji or something. By clicking on that, it opens up a debugger panel. That debugger panel lets you browse these different module or the different objects within modules to list all of them, even the ones that are maybe in the database, but are status of deleted or something. But that's a specific section of this AI to human visibility document that needs to specifically be called out. What is the visibility strategy for showing persisted objects? What is that for each one of them? The most important things humans want to be able to see. What is the simplified view I can show to make it easy and quick to browse this? What general things would I show? What's some idea of layout for each one of those? But then what is the button I can click on to view the raw JSON dump for a given object. We can just be smarter about that. So the other half is the logging things where I can enable logging to see what's going on. And the third is logging reviewer. I think that for right now, logs get captured, just need to have a log viewer I can view the logs in the page. Logs do need to be persisted into some long-term logging session-based thing. So if I load an application, it should start logging for that session. Those logs should be available to be viewed in there. But if I reload the page to do it again, I need the past logs also stored in there so I can go to the previous log session, be able to copy those things out from that. But the logging views themselves, I need to be able to kind of walk through a tour as to what happened during the given session. And generally the things that end up showing the log should be controllable, the logging views themselves. I need to be able to kind of walk through a tour as to what happened during the given session. And generally the things that end up showing the log should be controllable by me controlling which components and modules that I have enabled logging on. If I've done some work on choosing to only debug those ones, theoretically by following through the logs, I might, you know, maybe there's eventually a debugging level if we decide that there's too much detail in here. I need to see it in a simpler model. But making a harness at least so that humans can view this, that that logging view. is effective. So in this AI to human visibility markdown file, each module should be annotated as to what the main... If it's a front-end thing, what are the main front-end steps and modes and things that could be logged? Generally, anything that's going to have an asynchronous delay on it, anything that has... data involved with it, you know, those are points to be logged, to be enabled for the debugging thing. Generally, starting events is to when something loads, and then ending events, once it's actually loaded or adequate. But if total elapsed is sometimes also valuable, you know, like the total amount of time from the first content, first paint or something on a list interface or something like that. That's sometimes useful to see as a metric on there. But each module on the front end described the different events that would need to be debugged. And then give an idea for each one of those ones about how many iterations in the average load, page load that are going to happen. But each module on the front end describe the different events that would need to be debugged. And then give an idea for each one of those ones about how many iterations in the average page load that are going to happen there. Because we need to call out in advance if there's 20,000 of these ones that happen on a given page load, or something that happens on D-Balance or something. that's going to be too noisy and we don't want to do those. So each of those telemetry points for each of the front end components needs to be called out for the back end. Any different methods that happen as part of a different flow there for the back end need to be called out as to what things are useful to show on each one of those events. because they don't need to show the full object, like if it's an API on the back end. It shouldn't be API foo was called, these were the arguments passed in, and then here was a response object. That is way too generic. Each one of these needs to be focused on the idea of what practically would be the best way for humans to be able to understand what is happening on the back end, what is happening on the front end. and where. And one final thing when you're viewing the logs, and we have turned on logging for different individual components, there should be a filter view so that I can hide and show things. If I decided to log for three components, and as I'm browsing it, I decided that, One of these is way noisy and I don't really want to see it. I should be able to turn those. I'll hide and show those to hide those in the interface. But this human AI to human visibility layer is essentially effectively the tour of the factory. I can go through and see what the logging telemetry is by looking at this document. By having this document defined, the AI should know exactly what it needs to build. And at the end, when the actual application is running, the factory floor is whirring and it's doing all of its steps. and some objects are flowing through conveyor belts and through pipes. Theoretically, a human could then just walk through and look at it. Anyway, this overall is a framework. that I think can be applied to any given application from the different user interfaces to communicate the value, break it down into pieces, but then also on the application that gets built, the human needs to be able to walk the floor. They need to be able to see the factory. They need to see the pipelines. They need to understand generally what is the different parts of the system here. Go and actually build these two documents. Don't actually change any code to start making it so. And this needs to be a document, essentially. For right now, just define for this given project that I am pasting this in for it. But just define what you think these documents are. and the third document at the end will just be a recommended refactors to be able to accomplish the goals in these two And the third document at the end will just be a recommended refactors to be able to accomplish. the goals in these two documents. Once again, the main document you're going to make is critical paths Let's see, we'll call it flows and parts maybe. It's got the critical flows and it's also got the parts, the front end components, the back end modules. So flows and parts markdown file and then an AI to human visibility markdown file. That are two things that would be in the documentation folder. in any of these applications that are going to be built on this one. And for right now, we're going to have you make those. After we've done some refining, then we'll have you look at several different projects that I've done this on, look at the outputs, and we'll try and standardize on what these things should look at more specifically. Can you go and set that up for this repository for me? Thank you.
Okay, so I don't mind a lot of the design you've done on your thing. I think The thing that I want you to now capture is... The big prompt I gave you at the beginning of this conversation is essentially what I want a summary of. of captured in a document that lives in this repository. Trying to capture as many of the points there as possible should be easily 2000 words, I don't know, 800 words. I'm not sure exactly how many words are actually in there in my prompt, but you know, whatever, Two-thirds of that is, that should be your goal. But I want it to have the detail, I want it to have the descriptions in there. Because essentially, the three files that were created as part of this, the flow in parts, the AI to human visibility, and the suggested refactors, Um... are essentially an evergreen set of documents that I want to stay accurate. And I want to have this described as a standard. To say this is a standard on here, and I want that standard. Maybe it's a... modularization standard markdown file or AI ModulizationStandard.md file maybe. But at the top, I want it to have some very clear tenets. I mean, generally, if you were to distill a lot of the things in that document down to a couple cardinal rules, some of them would be things like 100% sure, every different component for the front end and the back end should be clearly defined. Now this is a litmus test that you can run. Theoretically the sequence of events is that This standard document is read. It then is understanding the current purposes of the app and use cases, if that's shifted at all. Then it creates those three documents. The parts and... flow, flow and parts. Then it creates the AI to human visibility. Then it creates the refactor plan. The refactor essentially is a living document keeping track of what The first two documents, if they change, it's because the nature or the purpose or the functionality of the application has shifted. Or there's inaccuracies in there. Those will get changed over time. It's kind of the standard we're trying to build. the AI to human visibility will change as we actually end up doing having problems with the app and we find out what visibility things were actually more useful. So that will ultimately update as we go along there. But then the recommended refactors is essentially a status of adherence to the first two documents. Have we actually completed it? Have we done everything? So that's the sequence. And then at that point, code changes are made. And then we can come back to the original standard again, look at the documents. see if everything is still good, update the current refactor recommendations on there. It might eventually get to the point where it says nothing to do with it. We are ideally, in which case, I should just brag about how we are really, really close to the ideals set forth in the original standards, AI Modulization Standard document. We should talk about how, you know, The flow is very debuggable. Humans can do this thing. And we've been doing this and it works great. And everything's completely separated. And the modules are all, back-end modules have really separate domains. And don't call into each other. Anyway, the theoretical words are grading thing. And anything that is inaccurate, we'll keep iterating on until that. current refactors thing is getting a satisfactory grade. The... But there should be some standards that should be true. Any of these apps, it should have, number one, an ability to go to some debugging panel and turn on debugging mode. It lists all the different components, front end and back end components. I can turn those on and off. Second, it's clearly defined in the AI to human flow that what the actual debugging experience should be when you're browsing those, what things you should see as you're looking at database objects, what things here should be, events you should see, the frequencies of them, you know. should be well defined on there. Ultimately, one of the other tenets definitely has to be, is all this instrumentation to do the debugging stuff implemented in such a way as to not impact performance if the debugging mode is disabled? Is it completely... disabled in a way that will not tank performance. It won't change any visual things if you turn on or off debugging. It needs to be invisible. and non-impactful when debugging is on. When debugging is on, it may, observation might degrade a little bit. I don't think it should show any visible changes. There might be acceptance to that rule, but when you turn on debugging, but it should be completely toggleable on or off, and it should have zero or very low risk of impacting performance by turning on the debugging thing. But then, yeah, ultimately the, you know, have all the different pieces. Are the flows functional? Another good thing eventually would be, are there... Is there a good way to actually assure that the critical flows, the secondary flows, tertiary flows are working? Some of them may be problematic to set up the thing, in which case we would say, yeah, we can't do an automated integration test for this. You would need browser integration. It would need something most of the time that's not a blocker. But, you know, that would be good to have as part of that, too. make sure that it is doing that. So these are just some simple tests. All those should essentially be tests that can be run on any of these things, that AI, modularization, standard, markdown file, should be written kind of generic. It shouldn't really mention anything about this specific project. It theoretically is a file we can copy and paste into any other project. Um... Do your best. And the recommended refactors markdown file should use these standard litmus testings. Yes, no. What values are we following? What is the value that that is adding for our debugging of this thing? Anyway, there's some refactoring there involved. Can you get that all figured out? Update that other document. And... if you need to update any of the standards. Like one of the standards I think should be used as a standard list is the parts. And flow should remain agnostic. I think right now in your project, you are mentioning the files that things are in. And the problem with that is, right now you're mentioning that a whole bunch of these things are in app.tsx, which is a problem and needs to be refactored. This shouldn't be about where things are now, but where the ideal is. So it should be staying agnostic that theoretically it is done. And if it is done correctly, the ubiquitous language, the modules, the parts, the flows... Should be good. Should make sense, not about what the app's current state is, but like the best actual user case here is. That eventually is going to trickle down to the recommended refactors. Once recommended refactors are complete, the place that these components and stuff are defined might shift. Things might be adjusted. And theoretically, that should not require any back edits necessarily. Back to the parts in flow markdown file. This should be defined as part of that standards AI Modulization Standard document that That's the purpose. That's the nature of that parts and flow document. It stays in the ubiquitous language thing. It's not specifically mentioning actual files or... where things are today. It's the application design, the needs of the user, the ubiquitous language. Make sure that's clearly called out in there. Uh, even the current refactor thing might even call that out to say, nope, it's not doing a good job doing that right now. So, um, that's one of the litmus test things is are these documents defined and are they following the standards? Um, yeah. Thank you.
No, I hate that you are making the AI  standard document nearly 100% ordered and bulleted lists. Much of the philosophy behind this standard should be paragraphs describing what I described to you try again I want  of 1:4 of lists versus paragraphs I do want some lists in there but four times as many words are in paragraphs  are in list
